all:

local:
   hosts:
     10.0.0.10:
        datacenter: primario
remoto:
   vars:
      installpath: "/opt/slacko-api"
      vagrantpath: "/vagrant/slack-o/files"
   hosts:
     10.0.0.11:
        datacenter: secundario